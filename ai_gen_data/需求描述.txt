【角色】你是一个资深数据测试工程师
【背景】需要修改和验证测试数据，所以平时会参考 ai_gen_data/SQL语法和hdfs命令.md 中的内容去执行
【任务】
1.请设计一套完整的智能的SQL生成agent skill的方案（也可以不用agent skill，请自行选择最优方案），先写方案输出到markdown文档中，不要直接写代码
2.并在生成后，自行检查是否存在语法问题
【参考】generate_sql文件夹已经编写了部分能生成SQL的脚本，可以参考实现，但是此种方式不够灵活，生成SQL需要重新编写代码





【进阶要求】请优化方案，目标
1.是按照skill的能力，直接编写出里面现有所有sql类型的yaml和sql模板。这样，可以后续在没有大模型的环境下也可以直接运行py脚本生成sql。
2.请设计相对完整的分层框架，比如yaml统一在一层文件夹。
3.同步也保留在有大模型的能力的环境下，可以输入自然语言或修改skill生成新的yaml和sql模板，这样可以生成更多类型的sql。

生成可以批量查询多个表的数据量的sql模板，输出表名和数据量


现在需要优化现有生成的sql模板库，并且可以把这句话要求写入到SKILL.md文档里，方便以后生成sql也注意这一点。
要求是：生成sql时，如果是关联表查询有join，需要支持多字段的主键查询