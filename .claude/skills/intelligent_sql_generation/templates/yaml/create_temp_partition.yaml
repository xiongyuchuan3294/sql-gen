type: "create_temp_partition"
description: "为分区表创建空白的 temp 分区"
params:
  table_name: "example_table"           # 表名
  db: "imd_aml_safe"                    # 数据库（可选）
  partition: "ds='2026-02-01-temp'"     # 分区值
